const coffeeStock = {
  arabica: 100,
  robusta: 150,
  liberica: 200

}

// const isCoffeeMachineReady = true;

// module.exports = { coffeeStock, isCoffeeMachineReady };
/**
 * if we want to export one value so we can write it
 * export default coffeeStock;
 */

// console.log(module);

// ES 6 MODULE

const stockCoffee = {
  arabica: 100,
  robusta: 150,
  liberica: 200,
}

export default stockCoffee;
